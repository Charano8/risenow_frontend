package com.simats.risenow.ui.theme

import androidx.compose.ui.graphics.Color

// ── Glassmorphism Clean UI Palette ──────────────────────────────────────────

// Background Gradients
val AppBackgroundTop    = Color(0xFF0F172A)
val AppBackgroundBottom = Color(0xFF1E293B)

// Glass Elements
val GlassWhite          = Color(0xFFFFFFFF).copy(alpha = 0.10f)
val GlassBorder         = Color(0xFFFFFFFF).copy(alpha = 0.20f)
val GlassShadow         = Color(0xFF000000).copy(alpha = 0.20f)

// Primary Gradient
val PrimaryGradientStart = Color(0xFF7C3AED)
val PrimaryGradientEnd   = Color(0xFF22D3EE)
val PrimaryGlow          = Color(0xFF22D3EE).copy(alpha = 0.3f)

// Text Colors
val TextPrimary          = Color(0xFFF8FAFC)
val TextSecondary        = Color(0xFFCBD5E1)
val TextDisabled         = Color(0xFF94A3B8)

// Onboarding Colors
val OnboardingBlue       = Color(0xFF3B82F6)
val OnboardingPurple     = Color(0xFFA855F7)
val OnboardingIndigo     = Color(0xFF6366F1)

// Semantic
val SuccessGreen = Color(0xFF22C55E)
val DangerRed    = Color(0xFFEF4444)
val WarnGold     = Color(0xFFFBBF24)